# Hi-HTTP 环境变量配置示例

# ================================
# API 地址配置
# ================================

# BUS 服务 API 地址
VITE_BUS_API_URL=http://your-api-domain.com/bus

# SYS 服务 API 地址  
VITE_SYS_API_URL=http://your-api-domain.com/sys

# 文件下载 API 地址
VITE_FILE_DOWNLOAD_API_URL=http://your-api-domain.com/download

# ================================
# 加密配置（可选）
# ================================

# 是否启用加密功能（默认: false）
# 设置为 true 启用，false 或不设置则禁用
VITE_ENCRYPT_ENABLED=false

# SM2 公钥（当启用加密时必需）
# 请替换为实际的 SM2 公钥
VITE_SM2PUBKEY=your_sm2_public_key_here

# 头部校验码（当启用加密时必需）
# 请替换为实际的头部校验码
VITE_HEADCRC=your_head_crc_here

# SM2 私钥（可选）
# 如果需要解密响应数据时设置
VITE_SM2PRIKEY=your_sm2_private_key_here

# 传输模式（默认: false）
# 设置为 true 启用传输模式
VITE_TRANSMISSION_MODE=false

# 加密调试模式（默认: false）
# 设置为 true 在控制台输出加密相关调试信息
VITE_ENCRYPT_DEBUG=false

# ================================
# 使用说明
# ================================

# 1. 复制此文件为 .env
# 2. 根据实际情况修改配置值
# 3. 如果不使用加密功能，只需配置 API 地址即可
# 4. 如果使用加密功能，需要安装 sm-core: npm install sm-core
